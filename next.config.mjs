/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
    domains: ["localhost"], // allow images from localhost (e.g. http://localhost:1337)
  },
};

export default nextConfig;
